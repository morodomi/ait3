---
id: '0022'
title: スタイルシステム再設計 - 4-5色のシンプル統一
status: done
priority: medium
created: '2025-07-08T03:00:32.362Z'
updated: '2025-07-08T09:15:26.108Z'
labels:
  - refactor
  - styles
  - architecture
started: '2025-07-08T08:05:05.807Z'
completed: '2025-07-08T09:15:26.108Z'
---
# Ticket #0022: スタイルシステム再設計 - 4-5色のシンプル統一

## Description

現在のFLOW_STYLESが複雑すぎるため、Bootstrap/Tailwind標準の4-5色シンプルスタイルシステムに再設計する。保守性と一貫性を大幅に向上させる。

## 現在の問題

### 複雑すぎるスタイル分類
```typescript
// 現在：25個以上のスタイル定数
FLOW_STYLES.gitSuccess, gitWarning, gitInfo, gitCommand
FLOW_STYLES.ticketId, ticketTitle, ticketStatus, statusTransition  
FLOW_STYLES.feature, section, command, option, progress
FLOW_STYLES.title, success, warning, error, info, path, count, code, dim
```

### 問題点
- **覚えにくい**: 25個以上のスタイル名
- **判断に迷う**: どのスタイルを使うべきか不明確
- **保守困難**: 似たような意味のスタイルが複数存在
- **意味論混在**: セマンティック/用途別/表示形式が混在

## 新設計提案

### シンプル4色システム（推奨）
```typescript
export const STYLES = {
  success: chalk.green,   // 成功操作（✅作成完了、ブランチ切り替え成功）
  info: chalk.blue,       // 情報表示（🔍検索結果、📋詳細情報）  
  warning: chalk.yellow,  // 注意・警告（⚠️警告、📝手動操作必要）
  danger: chalk.red,      // エラー・危険（❌失敗、🚫禁止操作）
  muted: chalk.gray      // 補助情報（コマンド例、ヘルプテキスト）
} as const;
```

### 使用例
```typescript
// Before (複雑)
FLOW_STYLES.gitSuccess('✓ ブランチ作成成功')
FLOW_STYLES.gitWarning('⚠️ フェッチ失敗')  
FLOW_STYLES.gitCommand('git checkout main')
FLOW_STYLES.statusTransition('todo → doing')

// After (シンプル)
STYLES.success('✓ ブランチ作成成功')
STYLES.warning('⚠️ フェッチ失敗')
STYLES.muted('git checkout main') 
STYLES.muted('todo → doing')
```

## 設計原則

### 1. セマンティック優先
- 見た目（色）ではなく意味（成功/警告等）で分類
- 将来的な色変更にも対応可能

### 2. 標準準拠
- Bootstrap/Tailwind/Material Design標準
- 他のUIライブラリとの一貫性

### 3. 判断簡単
- 成功→success、警告→warning、エラー→danger
- 迷いなく選択可能

## 実装計画

### Phase 1: 新STYLES定数作成
- `src/common/styles.ts`を簡素化
- 4-5色のシンプルシステム実装
- 既存FLOW_STYLESとの併存期間設定

### Phase 2: 段階的移行
- 新規開発は新STYLESを使用
- 既存コードの段階的置換
- チケット単位での移行（start, create, complete, list, show）

### Phase 3: 旧システム削除
- FLOW_STYLESの段階的削除
- 全コマンドの移行完了確認
- 不要なスタイル定数の除去

## 移行対象ファイル

### 高優先度（多用している）
- `src/commands/ticket/start.ts` - 25箇所以上
- `src/commands/ticket/create.ts` - 多数のchalk使用
- `src/commands/ticket/complete.ts` - ステータス表示
- `src/commands/ticket/list.ts` - 一覧表示
- `src/commands/ticket/show.ts` - 詳細表示

### 中優先度（flowコマンド）
- `src/commands/flow/*.ts` - 既存FLOW_STYLES使用中

## 成功基準

- [ ] 新STYLESシステム実装（4-5色）
- [ ] 全ticketコマンドの移行完了
- [ ] 色使用の一貫性確保
- [ ] 開発者ガイドライン作成
- [ ] 既存テスト全通過

## 期待効果

### 開発効率向上
- スタイル選択に迷わない
- 覚えやすい（4-5個のみ）
- 新機能開発の加速

### 保守性向上  
- 一箇所での色変更
- 意味の明確化
- コードレビューの簡素化

### ユーザビリティ向上
- 一貫した色使い
- 直感的な意味理解
- アクセシビリティ対応準備

## Technical Notes

- TypeScript strictモード対応
- chalk level設定の統一
- テスト環境での色表示対応
- エラーハンドリングとの整合性

## 要件

### 機能要件
- セマンティック5色システムの実装（success, info, warning, danger, muted）
- 既存25個のスタイル定数を7個に削減
- 全てのCLIコマンドで統一された色使い
- 既存機能への影響なし（出力の意味は維持）

### 非機能要件  
- TypeScript strictモード完全準拠
- 全349テストのパス維持
- 段階的移行による安全な実装
- chalkライブラリのみ使用（追加依存なし）

### 制約条件
- 色の意味は既存を踏襲（緑=成功、赤=エラー等）
- ANSIカラーコードの互換性維持
- テスト環境での色表示対応

## 仕様

### 新STYLESシステム定義
```typescript
// src/common/styles.ts に追加
export const STYLES = {
  // セマンティック5色
  success: chalk.green,    // 成功操作（作成完了、ブランチ切替成功等）
  info: chalk.blue,        // 情報表示（ファイルパス、ID、統計情報等）
  warning: chalk.yellow,   // 注意・警告（手動操作必要、非推奨等）
  danger: chalk.red,       // エラー・危険（失敗、検証エラー、例外等）
  muted: chalk.gray,       // 補助情報（コマンド例、ヒント、詳細等）
  
  // 基本装飾（最小限）
  bold: chalk.bold,        // 強調（タイトル、重要情報）
  code: chalk.gray         // コード表示（コマンド、ファイルパス）
} as const;

export type StylesType = typeof STYLES;
```

### 移行マッピングルール
```typescript
// FLOW_STYLES → STYLES 変換規則
FLOW_STYLES.success → STYLES.success
FLOW_STYLES.error → STYLES.danger
FLOW_STYLES.warning → STYLES.warning  
FLOW_STYLES.info → STYLES.info
FLOW_STYLES.gitSuccess → STYLES.success
FLOW_STYLES.gitWarning → STYLES.warning
FLOW_STYLES.gitInfo → STYLES.info
FLOW_STYLES.gitCommand → STYLES.muted
FLOW_STYLES.dim/code → STYLES.muted
FLOW_STYLES.path → STYLES.info
FLOW_STYLES.title/section → STYLES.bold
FLOW_STYLES.feature → STYLES.success
FLOW_STYLES.command/option → STYLES.info
FLOW_STYLES.count → STYLES.info
FLOW_STYLES.ticketId/ticketTitle/ticketStatus → テキストのみ（装飾なし）
FLOW_STYLES.statusTransition → STYLES.muted
```

### 段階的移行手順
1. **Phase 1**: 新STYLES実装とテスト作成
2. **Phase 2**: 直接chalk使用ファイルの移行（高優先度）
   - ticket/create.ts, complete.ts, list.ts, show.ts
3. **Phase 3**: FLOW_STYLES使用ファイルの移行
   - flow/*.ts, ticket/start.ts, common/flow-utils.ts
4. **Phase 4**: 旧システム削除と最終調整

### テスト方針
- 色の具体値は検証しない（ANSIコード存在確認のみ）
- 出力メッセージの意味・構造は維持
- ファイル単位でテスト実行し段階的に進行
